#! /bin/bash

echo "[i]> === ganon"
date

echo "[i]> N/A"

# # run_id=$1
# inputfile="$1"
# outdir="$2"

# outputfile=$(basename $inputfile)
# k2db=/rica_s/tools/rica_s_id_kraken2/pathogen.k2db/

# /usr/bin/time -v k2 classify --db $k2db --threads `nproc` --report $outputfile.kraken2.report --output $outputfile.kraken2.output --unclassified-out $outputfile.kraken2.unclassified --classified-out $outputfile.kraken2.classified $inputfile


date
echo "[i]> ganon ==="